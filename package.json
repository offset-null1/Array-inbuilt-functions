{
  "name": "with_babel",
  "version": "1.0.0",
  "description": "Practicing js using babel",
  "main": "index.js",
  "scripts": {
    "build": "babel src -d dist"
  },
  "scripts": {

      "build": "babel --presets env src -d dist"
  },
  "author": "dakshta",
  "license": "ISC",
  "devDependencies": {
    "babel-cli": "^6.26.0",
    "babel-preset-env": "^1.7.0"
  }
}
